- server setup

  ```bash
   npm init -y => create package.json file တစ်ခုတည်ဆောက်ရသည်/
   npm i express => install express dependency for create node server
   npm install nodemon -g => install nodemon automatic change when code is change
   npm install socket.io =>
  ```
